import React, { Fragment } from "react";
import LoginForm from "./LoginForm";

const Validation = () => {
  return (
    <Fragment>
      <LoginForm />
    </Fragment>
  );
};

export default Validation;
