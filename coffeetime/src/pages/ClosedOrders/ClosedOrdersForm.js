import React from "react";
import closedorders from "./closedorders.png";

const ClosedOrdersForm = () => (
  <main className="main">
    <img src={closedorders} alt={"closedorders"} />
  </main>
);

export default ClosedOrdersForm;
